# Node / Next.js
node_modules/
.next/
out/
dist/
.vercel/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
*.pyc

# Data cache
data/cache/
*.parquet

# Env secrets
.env
.env.local
.env.*.local
.streamlit/secrets.toml

# Scalpel local data (never commit DB or signing key)
*.db
scalpel/.secret_key
instance/
backups/

# OS
.DS_Store
Thumbs.db

# Reports output
reports/*.txt
reports/*.pdf

# Mentorship support material (member-only files, served through a gated route)
scalpel/private_uploads/
# Subidas del foro: datos de runtime (y las deja también el test
# tools/test_foro_imagenes.py al probar la subida permitida).
scalpel/static/uploads/

# Tipografias descargadas por tools/gen_posts_ig.py (SIL OFL, no son del producto)
tools/.fuentes/
