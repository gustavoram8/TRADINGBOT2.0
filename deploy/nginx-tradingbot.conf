server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    # Long timeouts: backtests can take several minutes to complete.
    proxy_connect_timeout 10s;
    proxy_send_timeout    600s;
    proxy_read_timeout    600s;

    # Generous buffers for the (potentially large) backtest JSON response.
    client_max_body_size  10M;
    proxy_buffer_size     16k;
    proxy_buffers         8 32k;
    proxy_busy_buffers_size 64k;

    # Next.js API routes — disable proxy buffering so "\n" keepalive bytes
    # sent by long-running routes (backtest, sleep) are forwarded immediately
    # to the browser instead of sitting in nginx's buffer.
    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_buffering    off;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Frontend (Next.js) — pages, assets, everything else
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Direct access to Python API (debug/diagnostics)
    location /api/py/ {
        proxy_pass http://127.0.0.1:8000/;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    }
}
