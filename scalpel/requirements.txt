flask==3.1.0
flask-sqlalchemy==3.1.1
flask-login==0.6.3
flask-mail==0.10.0
openai==1.82.0
python-dotenv==1.1.0
werkzeug==3.1.3
gunicorn==23.0.0

# Optional: error tracking. Only used if SENTRY_DSN env var is set.
sentry-sdk[flask]==2.20.0

# Optional: scannable QR on rank certificates. Pure-python, no deps. If absent,
# the certificate falls back to printing the verification code + URL as text.

# Optional: Stripe card payments. Only used when STRIPE_SECRET_KEY is set; the
# app imports it lazily and falls back to the manual USDT/bank flow if absent.
stripe>=11.0.0

# Chart screenshot downscaling before the vision API (caps per-analysis image
# cost). Imported lazily in normalize_chart_image(); if absent, the original
# image is sent unresized — analysis still works, just without the cost cap.
Pillow>=10.0.0
# QR for two-factor-auth enrollment ONLY (the certificate QRs stay dead)
segno>=1.6
